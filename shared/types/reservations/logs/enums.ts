export enum ReservationLogTypeEnum {
    CREATED = 'created',
    UPDATED = 'updated',
    CANCELLED = 'cancelled',
    ASSIGNED = 'assigned',
    COMPLETED = 'completed',
}


export enum UserTypeEnum {
    UNREGISTERED = 'unregistered',
    ADMIN = 'admin',
    DRIVER = 'driver',
}